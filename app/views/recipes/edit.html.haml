- @page_title = "Edit Recipe"

.flex.items-center.gap-3.mb-4
  = link_to recipe_path(@recipe), "aria-label": "Back to recipe", class: "flex items-center justify-center w-11 h-11 text-text-secondary hover:text-text rounded-lg hover:bg-primary-tint border border-border transition-colors" do
    != '<svg class="w-5 h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5"/></svg>'
  %h1.text-xl.font-bold.truncate Edit recipe

= render "form", recipe: @recipe, foods: @foods, url: recipe_path(@recipe), submit_label: "Save changes"
